{
  "name": "cloth_admin",
  "private": true,
  "version": "1.0.0",
  "description": "Yeshi Clothe admin panel — pure static HTML/CSS/JS, no build step required.",
  "scripts": {
    "start": "npx serve public -p 3001",
    "dev": "npx serve public -p 3001"
  }
}
